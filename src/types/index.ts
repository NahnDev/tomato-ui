import { TShape } from "@/components/share/maker/type";

export type nil<T> = undefined | null | T;

export type TSize = { width: number; height: number };
export type TCoord = { x: number; y: number };
